# Руководство по развертыванию через Coolify

## 📋 Подготовка

### 1. Требования

- Установленный и настроенный Coolify (v3+)
- Доступ к репозиторию GitHub/GitLab
- Минимум 2GB RAM на сервере
- 10GB+ свободного места на диске

### 2. Подготовка репозитория

Убедитесь, что в репозитории есть:
- ✅ `docker-compose.yml` или `docker-compose.coolify.yml`
- ✅ `Dockerfile` (для backend)
- ✅ `frontend/Dockerfile` (для frontend)
- ✅ Все необходимые файлы проекта

---

## 🚀 Пошаговое развертывание

### Шаг 1: Создание нового ресурса в Coolify

1. Войдите в панель Coolify
2. Перейдите в раздел **Resources** → **New Resource**
3. Выберите тип: **Docker Compose**
4. Назовите ресурс (например: `prosnabdb`)

### Шаг 2: Подключение репозитория

1. Выберите **Source**: GitHub/GitLab
2. Авторизуйтесь и выберите репозиторий `prosnabdb`
3. Укажите ветку (обычно `main` или `master`)
4. Укажите путь к docker-compose файлу:
   - Если используете `docker-compose.coolify.yml`, переименуйте его в `docker-compose.yml` в репозитории
   - Или укажите путь: `docker-compose.coolify.yml` (если Coolify поддерживает кастомный путь)

### Шаг 3: Настройка переменных окружения

В Coolify UI будут автоматически обнаружены все переменные из `docker-compose.yml`. Настройте следующие:

#### Обязательные переменные:

```env
# База данных
DATABASE_NAME=prosnab_db
DATABASE_USER=postgres
DATABASE_PASSWORD=your_secure_password_here  # ⚠️ ОБЯЗАТЕЛЬНО изменить!
DATABASE_PORT=5432

# Django
SECRET_KEY=your-django-secret-key-here  # ⚠️ Сгенерируйте новый!
DEBUG=0  # Для production используйте 0
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com  # Ваши домены

# Порты (опционально, если не используете домены)
BACKEND_PORT=8000
FRONTEND_PORT=80

# Celery (уже настроено, но можно изменить)
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0
```

#### Генерация SECRET_KEY:

```bash
# В терминале или через Coolify
python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
```

### Шаг 4: Настройка доменов (рекомендуется)

#### Для Frontend:
1. В Coolify UI найдите сервис `frontend`
2. Перейдите в **Domains**
3. Добавьте домен (например: `app.yourdomain.com`)
4. Coolify автоматически настроит SSL через Let's Encrypt

#### Для Backend API:
1. В Coolify UI найдите сервис `backend`
2. Перейдите в **Domains**
3. Добавьте домен (например: `api.yourdomain.com`)
4. Настройте SSL

**Важно:** После добавления доменов обновите `ALLOWED_HOSTS`:
```env
ALLOWED_HOSTS=app.yourdomain.com,api.yourdomain.com
```

### Шаг 5: Настройка Storage (Volumes)

Coolify автоматически создаст volumes из docker-compose.yml:
- `postgres_data` - данные PostgreSQL
- `redis_data` - данные Redis
- `media_volume` - медиа файлы
- `static_volume` - статические файлы

**Рекомендация:** Настройте автоматические бэкапы для `postgres_data` через Coolify или вручную.

### Шаг 6: Деплой

1. Нажмите **Deploy** в Coolify UI
2. Coolify автоматически:
   - Клонирует репозиторий
   - Соберет Docker образы
   - Запустит все сервисы
   - Применит миграции (через entrypoint.sh)

3. Следите за логами в реальном времени

---

## 🔧 Настройка после деплоя

### 1. Проверка работоспособности

```bash
# Проверить статус всех сервисов
# В Coolify UI: Resources → ваш ресурс → Services

# Проверить логи
# В Coolify UI: Resources → ваш ресурс → Logs
```

### 2. Создание суперпользователя Django

```bash
# В Coolify UI: Resources → ваш ресурс → backend → Terminal
python manage.py createsuperuser
```

### 3. Применение миграций (если нужно)

Миграции применяются автоматически через `entrypoint.sh`, но можно вручную:

```bash
# В Coolify UI: Resources → ваш ресурс → backend → Terminal
python manage.py migrate
python manage.py collectstatic --noinput
```

### 4. Проверка Celery Worker

```bash
# В Coolify UI: Resources → ваш ресурс → worker → Logs
# Должны быть сообщения типа: "celery@... ready"
```

---

## 🌐 Настройка Nginx для API проксирования

Если вы используете домены через Coolify, frontend уже настроен для проксирования API запросов к backend через `frontend/nginx.conf`.

Убедитесь, что в nginx.conf правильно указан адрес backend:
```nginx
location /api/ {
    proxy_pass http://backend:8000;
    # ...
}
```

---

## 📊 Мониторинг и логи

### Просмотр логов в Coolify:

1. **Все сервисы:**
   - Resources → ваш ресурс → Logs

2. **Конкретный сервис:**
   - Resources → ваш ресурс → Services → выберите сервис → Logs

3. **В реальном времени:**
   - Включите "Follow logs" в интерфейсе логов

### Полезные команды для отладки:

```bash
# Проверить подключение к базе данных
docker exec -it <backend_container> python manage.py dbshell

# Проверить Redis
docker exec -it <redis_container> redis-cli ping

# Проверить статус Celery
docker exec -it <worker_container> celery -A prosnabdb inspect active
```

---

## 🔄 Обновление приложения

### Автоматическое обновление:

Coolify может автоматически обновлять приложение при пуше в репозиторий (Webhook).

### Ручное обновление:

1. В Coolify UI: Resources → ваш ресурс
2. Нажмите **Redeploy**
3. Или нажмите **Pull & Deploy** для обновления кода

### После обновления:

```bash
# Применить новые миграции (автоматически через entrypoint.sh)
# Или вручную:
python manage.py migrate
python manage.py collectstatic --noinput
```

---

## 🗄️ Управление базой данных

### Бэкап базы данных:

```bash
# В Coolify UI: Resources → ваш ресурс → postgres → Terminal
pg_dump -U postgres prosnab_db > backup_$(date +%Y%m%d).sql

# Или через docker exec
docker exec <postgres_container> pg_dump -U postgres prosnab_db > backup.sql
```

### Восстановление:

```bash
# В Coolify UI: Resources → ваш ресурс → postgres → Terminal
psql -U postgres prosnab_db < backup.sql
```

### Доступ к базе данных извне:

Если нужно подключиться к БД из внешнего клиента:

1. В Coolify UI найдите сервис `postgres`
2. Добавьте порт mapping: `5432:5432` (временно)
3. Подключитесь используя:
   - Host: ваш_сервер_ip
   - Port: 5432
   - Database: prosnab_db
   - User: postgres
   - Password: из переменной DATABASE_PASSWORD

**⚠️ Безопасность:** Удалите порт mapping после использования!

---

## 🐛 Troubleshooting

### Проблема: Сервисы не запускаются

**Решение:**
1. Проверьте логи в Coolify UI
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что DATABASE_PASSWORD и SECRET_KEY установлены

### Проблема: Backend не может подключиться к PostgreSQL

**Решение:**
1. Проверьте, что `DATABASE_HOST=postgres` (имя сервиса)
2. Убедитесь, что postgres сервис запущен и healthy
3. Проверьте переменные DATABASE_* в Coolify UI

### Проблема: Frontend не может подключиться к Backend API

**Решение:**
1. Проверьте nginx.conf в frontend
2. Убедитесь, что backend сервис доступен по имени `backend:8000`
3. Проверьте CORS настройки в Django settings.py

### Проблема: Celery worker не работает

**Решение:**
1. Проверьте логи worker в Coolify UI
2. Убедитесь, что Redis доступен
3. Проверьте CELERY_BROKER_URL и CELERY_RESULT_BACKEND

### Проблема: Статические файлы не загружаются

**Решение:**
1. Проверьте, что volume `static_volume` смонтирован
2. Убедитесь, что `collectstatic` выполнен
3. Проверьте пути в nginx.conf

---

## 🔒 Безопасность

### Рекомендации для production:

1. **Измените все пароли:**
   - DATABASE_PASSWORD
   - SECRET_KEY (Django)

2. **Настройте DEBUG=0:**
   ```env
   DEBUG=0
   ```

3. **Ограничьте ALLOWED_HOSTS:**
   ```env
   ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com
   ```

4. **Используйте SSL:**
   - Настройте домены через Coolify для автоматического SSL

5. **Настройте firewall:**
   - Закройте все порты кроме 80, 443
   - PostgreSQL и Redis не должны быть доступны извне

6. **Регулярные бэкапы:**
   - Настройте автоматические бэкапы базы данных
   - Бэкапируйте volumes с медиа файлами

---

## 📝 Чеклист развертывания

- [ ] Репозиторий подключен к Coolify
- [ ] docker-compose.yml настроен для Coolify
- [ ] Все переменные окружения установлены
- [ ] SECRET_KEY сгенерирован и установлен
- [ ] DATABASE_PASSWORD изменен на безопасный
- [ ] DEBUG=0 для production
- [ ] ALLOWED_HOSTS настроен с вашими доменами
- [ ] Домены добавлены для frontend и backend
- [ ] SSL сертификаты получены (автоматически)
- [ ] Суперпользователь Django создан
- [ ] Миграции применены
- [ ] Celery worker работает
- [ ] Бэкапы настроены
- [ ] Мониторинг настроен

---

## 🎉 Готово!

После выполнения всех шагов ваше приложение должно быть доступно по настроенным доменам.

**Frontend:** `https://app.yourdomain.com`
**Backend API:** `https://api.yourdomain.com/api/`

---

## 📚 Дополнительные ресурсы

- [Coolify Documentation](https://coolify.io/docs)
- [Docker Compose в Coolify](https://coolify.io/docs/knowledge-base/docker/compose)
- [Django Deployment Checklist](https://docs.djangoproject.com/en/stable/howto/deployment/checklist/)

---

## 💡 Советы

1. **Используйте отдельные окружения:**
   - Создайте отдельный ресурс в Coolify для staging
   - Используйте разные ветки Git для dev/staging/production

2. **Мониторинг:**
   - Настройте алерты в Coolify
   - Используйте внешний мониторинг (UptimeRobot, Pingdom)

3. **Производительность:**
   - Настройте количество Celery workers в зависимости от нагрузки
   - Используйте кэширование Redis для Django

4. **Масштабирование:**
   - Coolify поддерживает горизонтальное масштабирование
   - Можно увеличить количество worker контейнеров
