# Быстрый старт: Оптимизированное развертывание в Coolify

## 🎯 Для серверов с ограниченными ресурсами

Если ваш сервер зависает при развертывании, используйте эту оптимизированную конфигурацию.

## ✅ Что было оптимизировано

1. **Ограничения ресурсов** - каждый сервис имеет лимиты CPU и памяти
2. **Последовательный запуск** - сервисы запускаются в правильном порядке
3. **Оптимизация worker** - worker не выполняет миграции (только backend)
4. **Увеличенные интервалы** - healthcheck проверки реже для снижения нагрузки

## 🚀 Быстрый старт

### 1. Используйте оптимизированный файл

В Coolify укажите путь к файлу: `docker-compose.coolify.yml`

### 2. Настройте переменные окружения

```env
DATABASE_NAME=prosnab_db
DATABASE_USER=postgres
DATABASE_PASSWORD=your_secure_password
SECRET_KEY=your-secret-key-here
DEBUG=0
ALLOWED_HOSTS=yourdomain.com
```

### 3. Поэтапное развертывание (рекомендуется)

#### Этап 1: База данных (2-3 мин)
- Запустите только `postgres` и `redis`
- Дождитесь зеленого статуса

#### Этап 2: Backend (3-5 мин)
- Запустите `backend`
- Дождитесь применения миграций и готовности

#### Этап 3: Остальные сервисы (2-3 мин)
- Запустите `worker` и `frontend`
- Дождитесь готовности всех сервисов

**Или:** Запустите все сразу - ограничения ресурсов предотвратят перегрузку.

### 4. Проверка

```bash
# В Coolify UI проверьте статус всех сервисов
# Все должны быть "Running" (зеленый)
```

## 📊 Ограничения ресурсов

- PostgreSQL: 1 CPU, 512MB RAM
- Redis: 0.5 CPU, 256MB RAM  
- Backend: 2 CPU, 1GB RAM
- Worker: 1 CPU, 512MB RAM
- Frontend: 0.5 CPU, 256MB RAM

## 🔧 Если все еще перегружается

1. Развертывайте поэтапно (см. шаг 3)
2. Уменьшите количество workers в `Dockerfile`:
   ```dockerfile
   CMD ["gunicorn", "...", "--workers", "2", ...]
   ```
3. Уменьшите concurrency в worker:
   ```yaml
   command: celery ... --concurrency=1
   ```

## 📚 Подробная документация

См. [COOLIFY_OPTIMIZED_DEPLOYMENT.md](./COOLIFY_OPTIMIZED_DEPLOYMENT.md) для полной документации.
