# Варианты конфигурации Nginx

## 📋 Два подхода к настройке Nginx

### Вариант 1: Nginx внутри frontend контейнера (текущий)

**Когда использовать:**
- Развертывание через Coolify или другие платформы
- Простая настройка без внешнего Nginx
- Frontend контейнер сам проксирует API запросы

**Конфигурация:**
- Используется `frontend/nginx.conf` (текущий файл)
- Frontend контейнер проксирует `/api/` и `/admin/` к backend
- Внешний Nginx просто проксирует все к frontend контейнеру

**Плюсы:**
- Все настройки в одном месте
- Проще для платформ типа Coolify

**Минусы:**
- Двойное проксирование (внешний Nginx → frontend nginx → backend)
- Меньше контроля над проксированием

---

### Вариант 2: Внешний Nginx на VPS (рекомендуется для VPS)

**Когда использовать:**
- Ручная настройка на VPS сервере
- Нужен полный контроль над проксированием
- Оптимизация производительности

**Конфигурация:**
- Используется `frontend/nginx.conf.simple` (упрощенная версия)
- Внешний Nginx на VPS проксирует все запросы напрямую:
  - `/` → frontend контейнер (localhost:3000)
  - `/api/` → backend контейнер (localhost:8000)
  - `/admin/` → backend контейнер (localhost:8000)
  - `/static/` → backend контейнер (localhost:8000)
  - `/media/` → backend контейнер (localhost:8000)

**Плюсы:**
- Прямое проксирование (без двойного проксирования)
- Лучшая производительность
- Полный контроль над SSL, кэшированием, безопасностью
- Можно использовать прямые пути к volumes для статики

**Минусы:**
- Требует настройки на VPS
- Больше конфигурационных файлов

---

## 🔄 Переключение между вариантами

### Использовать вариант 1 (текущий, для Coolify):

```bash
# Ничего менять не нужно
# frontend/nginx.conf уже настроен правильно
```

### Использовать вариант 2 (для VPS с внешним Nginx):

```bash
# В frontend/Dockerfile или при сборке
# Заменить nginx.conf на упрощенную версию
cp frontend/nginx.conf.simple frontend/nginx.conf

# Или изменить Dockerfile:
# COPY nginx.conf.simple /etc/nginx/conf.d/default.conf
```

---

## 📝 Рекомендации

### Для Coolify:
✅ Используйте вариант 1 (текущий `frontend/nginx.conf`)

### Для VPS с внешним Nginx:
✅ Используйте вариант 2 (`frontend/nginx.conf.simple` + `nginx/nginx.conf` на VPS)

---

## 🔧 Текущая конфигурация docker-compose.vps.yml

В `docker-compose.vps.yml` контейнеры настроены для варианта 2:
- Frontend доступен на `localhost:3000`
- Backend доступен на `localhost:8000`
- Внешний Nginx проксирует запросы напрямую

Если хотите использовать вариант 1, измените `docker-compose.vps.yml`:
- Frontend может проксировать API сам
- Внешний Nginx просто проксирует все к frontend

---

## 💡 Оптимальная конфигурация для VPS

Рекомендуется использовать **вариант 2** с внешним Nginx, так как:
1. Лучшая производительность (прямое проксирование)
2. Централизованное управление SSL
3. Возможность оптимизации кэширования
4. Лучший контроль безопасности
