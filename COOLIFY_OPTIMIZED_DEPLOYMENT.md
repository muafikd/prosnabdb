# Оптимизированное развертывание через Coolify

## 🎯 Проблема

При развертывании проекта на VPS сервере с ограниченными ресурсами может возникать перегрузка сервера из-за:
- Одновременной сборки всех Docker образов (backend, frontend, worker)
- Параллельного запуска всех сервисов
- Выполнения миграций и collectstatic в нескольких контейнерах одновременно
- Отсутствия ограничений ресурсов

## ✅ Решение

Конфигурация `docker-compose.coolify.yml` была оптимизирована для постепенного развертывания:

### 1. Ограничения ресурсов

Каждый сервис имеет ограничения CPU и памяти:

- **PostgreSQL**: максимум 1 CPU, 512MB RAM
- **Redis**: максимум 0.5 CPU, 256MB RAM
- **Backend**: максимум 2 CPU, 1GB RAM
- **Worker**: максимум 1 CPU, 512MB RAM
- **Frontend**: максимум 0.5 CPU, 256MB RAM

Это предотвращает перегрузку сервера при развертывании.

### 2. Последовательный запуск сервисов

Сервисы запускаются в правильном порядке с проверками здоровья:

1. **PostgreSQL** и **Redis** запускаются первыми
2. **Backend** ждет готовности PostgreSQL и Redis
3. **Worker** ждет готовности всех зависимостей
4. **Frontend** ждет готовности Backend

### 3. Оптимизация entrypoint.sh

- **Backend**: выполняет миграции и collectstatic
- **Worker**: пропускает миграции и collectstatic (через переменную `SKIP_MIGRATIONS=1`)

Это исключает дублирование операций и снижает нагрузку.

### 4. Увеличенные интервалы healthcheck

- Увеличены интервалы проверки здоровья для снижения нагрузки
- Увеличены периоды ожидания перед первой проверкой (start_period)

## 🚀 Пошаговое развертывание

### Шаг 1: Подготовка

Убедитесь, что у вас есть:
- Минимум 2GB RAM на сервере
- 10GB+ свободного места на диске
- Установленный Coolify (v3+)

### Шаг 2: Создание ресурса в Coolify

1. Войдите в панель Coolify
2. Перейдите в **Resources** → **New Resource**
3. Выберите тип: **Docker Compose**
4. Назовите ресурс (например: `prosnabdb`)

### Шаг 3: Подключение репозитория

1. Выберите **Source**: GitHub/GitLab
2. Авторизуйтесь и выберите репозиторий
3. Укажите ветку (обычно `main` или `master`)
4. Укажите путь к docker-compose файлу: `docker-compose.coolify.yml`

### Шаг 4: Настройка переменных окружения

#### Обязательные переменные:

```env
# База данных
DATABASE_NAME=prosnab_db
DATABASE_USER=postgres
DATABASE_PASSWORD=your_secure_password_here  # ⚠️ ОБЯЗАТЕЛЬНО изменить!
DATABASE_PORT=5432

# Django
SECRET_KEY=your-django-secret-key-here  # ⚠️ Сгенерируйте новый!
DEBUG=0  # Для production используйте 0
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com  # Ваши домены

# Порты (опционально, если не используете домены)
BACKEND_PORT=8000
FRONTEND_PORT=80
```

#### Генерация SECRET_KEY:

```bash
python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
```

### Шаг 5: Постепенное развертывание

**Важно:** Для снижения нагрузки рекомендуется развертывать сервисы поэтапно:

#### Этап 1: База данных и Redis (первые 2-3 минуты)

1. В Coolify UI найдите ваш ресурс
2. Остановите все сервисы кроме `postgres` и `redis`
3. Запустите только эти два сервиса
4. Дождитесь их полной готовности (зеленый статус в Coolify)

#### Этап 2: Backend (следующие 3-5 минут)

1. После готовности postgres и redis, запустите `backend`
2. Backend автоматически:
   - Применит миграции
   - Соберет статические файлы
   - Запустится и пройдет healthcheck
3. Дождитесь готовности backend (зеленый статус)

#### Этап 3: Worker и Frontend (последние 2-3 минуты)

1. После готовности backend, запустите `worker` и `frontend`
2. Worker не будет выполнять миграции (оптимизация)
3. Дождитесь готовности всех сервисов

**Альтернатива:** Если вы уверены в ресурсах сервера, можно запустить все сразу - ограничения ресурсов предотвратят перегрузку.

### Шаг 6: Проверка работоспособности

```bash
# В Coolify UI: Resources → ваш ресурс → Services
# Все сервисы должны быть в статусе "Running" (зеленый)

# Проверка логов
# В Coolify UI: Resources → ваш ресурс → Logs
```

### Шаг 7: Создание суперпользователя

```bash
# В Coolify UI: Resources → ваш ресурс → backend → Terminal
python manage.py createsuperuser
```

## 🔧 Дополнительные оптимизации

### Если сервер все еще перегружается

1. **Уменьшите количество workers в backend:**
   В `Dockerfile` измените:
   ```dockerfile
   CMD ["gunicorn", "prosnabdb.wsgi:application", "--bind", "0.0.0.0:8000", "--workers", "2", "--timeout", "120"]
   ```

2. **Уменьшите concurrency в worker:**
   В `docker-compose.coolify.yml` измените:
   ```yaml
   command: celery -A prosnabdb worker -l info --concurrency=1
   ```

3. **Еще больше ограничьте ресурсы:**
   В `docker-compose.coolify.yml` уменьшите значения в `deploy.resources.limits`

### Мониторинг ресурсов

```bash
# На сервере проверьте использование ресурсов
docker stats

# Или через Coolify UI: Resources → ваш ресурс → Metrics
```

## 📊 Ожидаемое время развертывания

- **PostgreSQL + Redis**: 2-3 минуты
- **Backend** (с миграциями): 3-5 минут
- **Worker**: 1-2 минуты
- **Frontend**: 1-2 минуты

**Общее время:** 7-12 минут при поэтапном развертывании

## 🐛 Troubleshooting

### Проблема: Сервер все еще зависает

**Решение:**
1. Убедитесь, что используете оптимизированный `docker-compose.coolify.yml`
2. Развертывайте поэтапно (см. Шаг 5)
3. Проверьте доступные ресурсы сервера: `free -h` и `df -h`
4. Уменьшите ограничения ресурсов еще больше

### Проблема: Сервисы не запускаются

**Решение:**
1. Проверьте логи в Coolify UI
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что DATABASE_PASSWORD и SECRET_KEY установлены
4. Увеличьте `start_period` в healthcheck, если сервисы медленно стартуют

### Проблема: Worker не работает

**Решение:**
1. Проверьте логи worker в Coolify UI
2. Убедитесь, что переменная `SKIP_MIGRATIONS=1` установлена для worker
3. Проверьте, что Redis доступен
4. Проверьте CELERY_BROKER_URL и CELERY_RESULT_BACKEND

## 📝 Чеклист оптимизированного развертывания

- [ ] Используется `docker-compose.coolify.yml` (оптимизированная версия)
- [ ] Все переменные окружения установлены
- [ ] SECRET_KEY сгенерирован и установлен
- [ ] DATABASE_PASSWORD изменен на безопасный
- [ ] DEBUG=0 для production
- [ ] ALLOWED_HOSTS настроен с вашими доменами
- [ ] Развертывание выполнено поэтапно (или все сразу с ограничениями)
- [ ] Все сервисы в статусе "Running"
- [ ] Суперпользователь Django создан
- [ ] Миграции применены (автоматически через backend)
- [ ] Celery worker работает
- [ ] Мониторинг ресурсов настроен

## 🎉 Готово!

После выполнения всех шагов ваше приложение должно быть развернуто без перегрузки сервера.

**Frontend:** `https://app.yourdomain.com`
**Backend API:** `https://api.yourdomain.com/api/`

---

## 💡 Советы

1. **Первый деплой:** Всегда выполняйте поэтапно для проверки
2. **Последующие деплои:** Можно запускать все сразу, так как образы уже собраны
3. **Мониторинг:** Следите за использованием ресурсов через Coolify Metrics
4. **Бэкапы:** Настройте автоматические бэкапы для `postgres_data` volume

## 📚 Дополнительные ресурсы

- [Coolify Documentation](https://coolify.io/docs)
- [Docker Compose в Coolify](https://coolify.io/docs/knowledge-base/docker/compose)
- [Docker Resource Limits](https://docs.docker.com/config/containers/resource_constraints/)
